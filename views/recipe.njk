{% extends "layout.njk" %}

{% block content %}
   <section>
      <img src="{{receita.image}}" alt="{{receita.title}}" class="recipe-banner" />
      <p class="title">{{receita.title}}</p>
      <p class="author">por {{receita.author}}</p>

      <p class="recipe-subtitle">Ingredientes <span id="hide-ingredients" class="hide">ESCONDER</span></p>
      <p  class="recipe-text">
        <ul id="ingredients" style="display: block">
            {% for ingredient in receita.ingredients %}
                    <li class="ingredient"> {{ingredient}} </li>
            {% endfor %}
        </ul>
      </p>

      <p class="recipe-subtitle">Modo de preparo <span id="hide-prepare-mode" class="hide">ESCONDER</span></p>
      <p id="prep" class="recipe-text">{{receita.preparation}}</p>

      <p class="recipe-subtitle">Informações adicionais <span id="hide-info" class="hide">ESCONDER</span></p>
      <p id="info" class="recipe-text">{{receita.information}}</p>
    </section>
{% endblock %}

{% block script %}
  <script src="scripts.js"></script>
{% endblock %}
